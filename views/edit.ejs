<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EditHub</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.45);
        z-index: -1;
      }
    </style>
  </head>

  <body
    class="min-h-screen bg-fixed bg-cover bg-center text-white flex flex-col items-center justify-center p-6"
    style="background-image: url('/images/bg.png');"
  >
    <!-- Back Button -->
    <button
      onclick="window.location.href='/'"
      class="absolute top-8 left-8 px-5 py-2 bg-blue-700 hover:bg-blue-900 rounded-lg font-semibold transition-all duration-300 shadow-lg"
    >
      ← Back
    </button>

    <!-- Edit Form Container -->
    <div
      class="relative w-full max-w-2xl rounded-2xl overflow-hidden shadow-2xl border border-white/10"
    >
      <!-- Subtle Background -->
      <div
        class="absolute inset-0 bg-cover bg-center"
        style="background-image: url('/images/section.svg'); filter: brightness(1.1) contrast(1.05);"
      ></div>

      <!-- Glass Layer -->
      <div class="absolute inset-0 bg-black/40 backdrop-blur-md"></div>

      <!-- Form -->
      <form
        action="/edit"
        method="POST"
        class="relative flex flex-col gap-6 p-10"
      >
        <h1 class="text-3xl font-bold text-center mb-2 tracking-wide">
          Edit File
        </h1>

        <!-- Hidden Old Filename -->
        <input type="hidden" name="previousName" value="<%= filename %>" />

        <!-- Title -->
        <div class="flex flex-col">
          <label class="font-semibold mb-2">File Title</label>
          <input
            name="newName"
            type="text"
            value="<%= filename %>"
            class="p-4 rounded-lg bg-white/10 text-white placeholder:text-gray-300 outline-none focus:ring-2 focus:ring-blue-400 transition"
            required
          />
        </div>

        <!-- Description -->
        <div class="flex flex-col">
          <label class="font-semibold mb-2">Description</label>
          <textarea
            name="description"
            rows="6"
            placeholder="Enter file description..."
            class="p-4 rounded-lg bg-white/10 text-white placeholder:text-gray-300 outline-none focus:ring-2 focus:ring-blue-400 resize-none transition"
            required
          ><%= description %></textarea>
        </div>

        <!-- Buttons -->
        <div class="flex justify-end gap-4 mt-4">
          <button
            type="button"
            onclick="window.location.href='/'"
            class="px-6 py-3 bg-gray-700 hover:bg-gray-600 rounded-lg font-semibold transition"
          >
            Cancel
          </button>
          <button
            class="px-6 py-3 bg-yellow-600 hover:bg-yellow-800 rounded-lg font-bold transition-colors duration-300"
          >
            Save Changes
          </button>
        </div>
      </form>
    </div>
    <footer class="mt-20 text-sm text-gray-400 pb-6">
      Built with 💜 by Ayesha
    </footer>
  </body>
</html>
